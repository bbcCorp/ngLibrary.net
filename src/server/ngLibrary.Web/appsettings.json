{
  "ClientApp": {
    "host": "localhost",
    "port": "4200"
  },

  "NotificationConf": {
    "SmtpServer": {
      "SmtpHost": "GET FROM ENV",
      "SmtpPort": "GET FROM ENV",
      "EnableSSL": "GET FROM ENV",
      "UserID": "GET FROM ENV",
      "Password": "GET FROM ENV"
    },

    "DefaultNotificationAddress": ["someone@somewhere"]
  },

  "ConnectionStrings": {
    "ngLibraryDbConnection": "GET FROM ENV"
  },

  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  }
}
