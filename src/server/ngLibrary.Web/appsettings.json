{
  "ClientApp": {
    "host": "localhost",
    "port": "4200"
  },

  "NotificationConf": {
    "SmtpServer": {
      "SmtpHost": "GET FROM ENV",
      "SmtpPort": "GET FROM ENV",
      "EnableSSL": "GET FROM ENV",
      "UserID": "GET FROM ENV",
      "Password": "GET FROM ENV"
    },

    "DefaultNotificationAddress": ["someone@somewhere"]
  },

  "ConnectionStrings": {
    "ngLibraryDbConnection": "Server=127.0.0.1;Port=5432;Database=ngLibrary;User Id=nglibapp;Password=somepwd;"
  },

  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  }
}
